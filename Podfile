# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LXDrownMenuModule' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'LXDrownMenuManager', :path => './'

  # Pods for LXDrownMenuModule

  target 'LXDrownMenuModuleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LXDrownMenuModuleUITests' do
    # Pods for testing
  end

end
